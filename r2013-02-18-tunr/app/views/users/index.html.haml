#users
  %table
    - @users.each do |user|
      %tr{:class => cycle('row0','row1')}
        %td
          - if user.is_admin
            %span.label.alert= user.name
          - else
            %span.label.success= user.name
        %td= number_to_currency(user.balance)
        %td= image_tag(user.image)
        %td= link_to('Delete', user, :method => 'delete', :confirm => 'Are you sure?', :class => 'button radius alert tiny') if user != @auth

