There are <%= pluralize(@person_count1, "ox") %> signed up.

There are <%= pluralize(@person_count2, "ox") %> signed up.

<br>

<%= truncate(@story, :length => 20) %>

<ul>
<% @numbers.each do |num| %>
  <li style="background-color:<%= cycle("red", "green", "orange", "purple") %>;"><%= num %></li>
<% end %>
</ul>
